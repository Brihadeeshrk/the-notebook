[{"content":"import { screen, render, within } from '@testing-library/react';\n\nfunction FormData() {\n  return (\n    <>\n      <button>go back</button>\n      <form aria-label=\"form\">\n        <button>Save</button>\n        <button>Cancel</button>\n      </form>\n    </>\n  );\n}\n\nrender(<FormData />)","type":"code","id":"12lgh"},{"content":"function toContainRole(container, role, quantity = 1) {\n  let pass = false\n\n  const elements = within(container).queryAllByRole(role)\n\n  if(elements.length === quantity) {\n    return {\n      pass: true,\n    }\n  }\n\n  return {\n    pass,\n    message: () => `expected to find ${quantity} ${role} elements, but found ${elements.length} instead`\n  }\n}\n\nexpect.extend({toContainRole})","type":"code","id":"1jtua"},{"content":"test(\"the form displays 2 buttons\", () => {\n  render(<FormData />)\n\n  const form = screen.getByRole('form')\n  // const buttons = within(form).getAllByRole('button')\n\n  // expect(buttons).toHaveLength(2)\n  expect(form).toContainRole('button', 2)\n})","type":"code","id":"e12vd"}]